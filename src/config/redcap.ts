import { RedcapAppConfig } from '../api/configmodels/RedcapAppConfig';
/** Hook-level defaults. Brand overrides are resolved exclusively through AppConfig. */
export const redcap = new RedcapAppConfig();
